
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod properties
mod_version=1.0.0
maven_group=dev.bobodado
archives_base_name=acquiredutils

# --- VERSION PINS: I could not reach maven.fabricmc.net / Mojang's piston-meta from this
# sandbox (network is allow-listed and does not include Fabric/Mojang hosts), so these
# numbers were NOT verified against the actual maven metadata. They're my best-known
# values as of the "Fabric for Minecraft 1.21.11" announcement (Loom 1.14, Loader 0.18.1).
# CONFIRM these against https://fabricmc.net/develop/ and https://modrinth.com/mod/fabric-api
# before your first build — Loom will fail fast and clearly if any of these are wrong.
minecraft_version=1.21.11
loader_version=0.18.1
loom_version=1.14.+
# Fabric API version string for 1.21.11 was not verifiable from here — check modrinth.com/mod/fabric-api
# and pick the build tagged for 1.21.11, then replace this placeholder.
fabric_version=0.141.6+1.21.11
